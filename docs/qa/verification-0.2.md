# Проверка 0.2 · 15 сентября 2026

## Автоматические проверки финального кода

`pnpm check`: PASS, код выхода 0. TypeScript: PASS. Vitest: 39/39, четыре файла. Prettier: PASS. Production build: PASS.

20 тестов исходного domain, 8 мастерской, 8 persistence, 3 React UI (jsdom; Scene замокан). Проверены четыре комбинации места/инструментов, порядок шагов, отказ двойного finish, отсутствие награды за повтор, отмена с сохранением готовой работы, неизменяемое имя автора, частичный черновик после переоткрытия IndexedDB, миграция v1→v2 с последующей записью. UI-проба отказа сохранения доказывает: artwork не появляется, draft остаётся, повтор успешно сохраняет. Старые проверки конфликтов вкладок, повреждённого current, явного восстановления и неизвестной будущей схемы продолжают проходить.

## Реальный браузер: локальный Vite

Пройдено во встроенном Chromium через UI:

1. Существующее сохранение 0.1 открылось: Мох, шарф, reading, одно воспоминание, 140 монет сохранились.
2. Без места/материалов кнопка начала заблокирована. Выбор подушки + карандашей открывает чистый лист. Цвет до мотива недоступен.
3. Выбран лист → перезагрузка страницы → мастерская: лист остаётся выбранным.
4. Шалфей → завершение: работа отображается в комнате и панели, 140 монет не меняются.
5. Табурет + штампы → звезда + глина → завершение: новая работа заменяет старую.
6. Перезагрузка → звезда/глина восстановлены.
7. Viewport 390×844 и 320×800: ширина документа равна доступной ширине (375/375 и 305/305 с учётом полосы прокрутки), горизонтального overflow нет. Viewport сброшен после проверки.
8. Визуально проверены desktop и mobile. [Desktop](desktop-0.2.png), [mobile](mobile-0.2.png).

В журнале dev-вкладки есть две старые ошибки HMR промежуточной синтаксической правки App.tsx; ошибка исправлена, финальная сборка проходит. Не объявляем весь накопленный журнал пустым.

## Production preview

На http://127.0.0.1:4173/ открыта собранная dist-версия с новым локальным сохранением (другой origin). Через UI пройдены подушка → карандаши → чистый лист → лист → шалфей → завершение. Готовая работа показана, баланс остался 120. Журнал ошибок/предупреждений этой вкладки: пуст. Это дополнительный smoke test сборки, не повтор всех dev-проб.

## Ограничения доказательств

Это локальная проверка, не публикация/CI и не независимый аудит. Реальные Android/iOS, offline install/PWA, screen reader, touch-жесты, слабый GPU, квота реального хранилища и длительная производительность не проверялись. Ошибки записи моделируются тестом; в браузере диск не заполнялся. Удержание и удовольствие не измерены.

Сборщик предупреждает о двух комментариях Zod и renderer chunk 557.31 kB (gzip 162.78 kB). Main JS 345.21 kB (gzip 106.20 kB). Это открытая задача оптимизации, не подавленное предупреждение.

## Состояние поставки

Локальная ветка main без коммитов, remote отсутствует. Сеть игры не требует внешних сервисов. Создание GitHub, push и публикация не выполнялись. Архив старого плана cozy-pet-plan.zip — предыдущий документ, не актуальный дистрибутив кода. Точка входа актуального проекта — README.md в little-hearth-companion.
